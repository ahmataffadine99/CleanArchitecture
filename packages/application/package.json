{
  "name": "@ecoeats/application",
  "version": "1.0.0",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsup src/index.ts --format cjs --dts",
    "dev": "tsup src/index.ts --format cjs --dts --watch"
  },
  "dependencies": {
    "@ecoeats/domain": "*"
  },
  "devDependencies": {
    "tsup": "^8.1.0"
  }
}
